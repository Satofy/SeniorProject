# Frontend configuration
# Mode can be 'mock' to use Next.js app routes (in-memory), or 'real' to call the rcd-auth-server
NEXT_PUBLIC_DATA_MODE=mock

# When NEXT_PUBLIC_DATA_MODE=real, this must point to the rcd-auth-server URL
# Example for local dev
NEXT_PUBLIC_API_URL=http://localhost:3002

# Optional: UI theme or other feature flags can go here
